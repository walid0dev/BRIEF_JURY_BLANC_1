info:
  name: Create invoice
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}"
  body:
    type: json
    data: |-
      {
        "supplierId":"69d902f8d02789c08b0d1a76",
        "amount":100,
        "dueDate":"{{$randomDateFuture}}",
        "description":"test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
