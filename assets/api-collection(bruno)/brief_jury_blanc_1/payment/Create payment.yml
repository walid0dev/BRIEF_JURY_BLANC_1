info:
  name: Create payment
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}{{invoiceId}}/payments"
  body:
    type: json
    data: |-
      {
      "amount":90
      }
  auth: inherit

runtime:
  variables:
    - name: invoiceId
      value: 69d90826d4e74255415999b2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
