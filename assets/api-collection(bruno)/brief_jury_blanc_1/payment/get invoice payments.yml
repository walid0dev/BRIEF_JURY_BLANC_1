info:
  name: get invoice payments
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}{{invoiceId}}/payments"
  auth: inherit

runtime:
  variables:
    - name: invoiceId
      value: 69d90826d4e74255415999b2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
