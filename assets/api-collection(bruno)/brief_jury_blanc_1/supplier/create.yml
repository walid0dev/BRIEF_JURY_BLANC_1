info:
  name: create supplier
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}"
  body:
    type: json
    data: |-
      {
        "name": "Acme Supply Co",
        "email": "supplier@example.com",
        "phone": "+1 555 0100",
        "address": "123 Market Street",
        "contact": "Jane Doe"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5