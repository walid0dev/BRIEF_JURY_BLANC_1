info:
  name: delete supplier
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{URL}}{{supplier_id}}"
  auth: inherit

runtime:
  variables:
    - name: supplier_id
      value: 000000000000000000000000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5